// import { fromJS } from 'immutable';
// import { makeSelectFDomain } from '../selectors';

// const selector = makeSelectFDomain();

describe('makeSelectFDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
